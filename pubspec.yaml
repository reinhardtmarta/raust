name: raust
description: Assistente pessoal com IA local (Llama)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  llamadart: ^0.6.15
  path_provider: ^2.1.4
  shared_preferences: ^2.3.0
  flutter_markdown: ^0.7.4   # Para mostrar respostas bonitas

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
