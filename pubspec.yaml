name: raust
description: Radar User Safe Travel - Alertas comunitários anônimos para RS.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  http: ^1.1.0
  firebase_core: ^2.24.2
  cloud_firestore: ^4.14.0
  # firebase_functions: ^4.5.9 # Comentado para evitar conflitos de versão iniciais
  firebase_messaging: ^14.7.10
  image_picker: ^1.0.4
  uuid: ^4.2.1
  flutter_tts: ^3.8.3
  shared_preferences: ^2.2.2
  geolocator: ^10.1.0
  intl: ^0.18.1
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
